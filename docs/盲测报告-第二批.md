# LoL 版本公告问答 · 离线评测

生成时间：2026-09-21 13:47　语料：4995 条（15.13—26.18）　案例：40 条

> **口径**：案例由作者按当前语料编写，与语料同源，**非盲测、无留出集**；命中@K 用引擎实际返回的证据列表判定，数值正确性用渲染出的回答文本判定。

## 关键结论

- Jev 重排没有改变本案例集的命中@1；两种口径打平。
- Jev 成本：7 次调用合计 $0.00069（均 $0.000099），相对本地检索可忽略；失败时会退回 BM25+向量排序并在轨迹里标注。
- 限制：案例同源、样本量小、差异由个别案例驱动，不能当作真实用户准确率；要外推需要独立盲测集。

### hybrid

- 单点问题 20 条（其中 14 条问题写了版本，按问题判定版本正确性）：命中@1 **20/20**、命中@5 **20/20**、版本正确 **14/14**、数值正确 **20/20**
- 语料缺口：2 条案例的预期值在语料里找不到（不计入上面的分母，说明该改动没被收录或与国服公告数值不同）
- 汇总问题 6 条：答案含预期对象 **6/6**
- 本版无记录 3 条：正确拒答/追问 **3/3**
- 越界/超范围 3 条：正确拒答/追问 **3/3**
- 跨版本聚合 6 条：时间线覆盖预期版本 **6/6**

### hybrid+jev

- 单点问题 20 条（其中 14 条问题写了版本，按问题判定版本正确性）：命中@1 **20/20**、命中@5 **20/20**、版本正确 **14/14**、数值正确 **20/20**
- 语料缺口：2 条案例的预期值在语料里找不到（不计入上面的分母，说明该改动没被收录或与国服公告数值不同）
- 汇总问题 6 条：答案含预期对象 **6/6**
- 本版无记录 3 条：正确拒答/追问 **3/3**
- 越界/超范围 3 条：正确拒答/追问 **3/3**
- 跨版本聚合 6 条：时间线覆盖预期版本 **6/6**
- 回答自检（Jev noul）：20 条回答，最低支持度 ≥ 0.5 的 **20/20**
- Jev：调用 7 次，合计 $0.00069（均 $0.000099）

## 案例明细（hybrid+jev）

| 案例 | 类型 | 状态 | 详情 |
|---|---|---|---|
| c01 | single | verify | 命中位次 1；版本正确；数值正确；自检 0.89 |
| c02 | single | verify | 命中位次 1；数值正确；自检 0.95 |
| c03 | single | abstained | **语料缺口** |
| c04 | single | verify | 命中位次 1；版本正确；数值正确；自检 0.91 |
| c05 | single | verify | 命中位次 1；版本正确；数值正确；自检 0.86 |
| c06 | single | verify | 命中位次 1；版本正确；数值正确；自检 0.93 |
| c07 | single | verify | 命中位次 1；版本正确；数值正确；自检 0.82 |
| c08 | single | verify | 命中位次 1；数值正确；自检 0.91 |
| c09 | single | verify | 命中位次 1；版本正确；数值正确；自检 0.90 |
| c10 | single | verify | 命中位次 1；版本正确；数值正确；自检 0.86 |
| c11 | single | verify | 命中位次 1；版本正确；数值正确；自检 0.93 |
| c12 | single | verify | 命中位次 1；数值正确；自检 0.94 |
| c13 | single | verify | 命中位次 1；版本正确；数值正确；自检 0.92 |
| c14 | single | verify | 命中位次 1；版本正确；数值正确；自检 0.90 |
| c15 | single | verify | 命中位次 1；数值正确；自检 0.81 |
| c16 | single | verify | 命中位次 1；版本正确；数值正确；自检 0.89 |
| c17 | single | verify | 命中位次 1；版本正确；数值正确；自检 0.90 |
| c18 | single | verify | 命中位次 1；版本正确；数值正确；自检 0.91 |
| c19 | single | verify | 命中位次 1；数值正确；自检 0.84 |
| c20 | single | abstained | **语料缺口** |
| c21 | single | verify | 命中位次 1；数值正确；自检 0.91 |
| c22 | single | verify | 命中位次 1；版本正确；数值正确；自检 0.87 |
| o01 | overview | verify | 含预期对象 |
| o02 | overview | verify | 含预期对象 |
| o03 | overview | verify | 含预期对象 |
| o04 | overview | verify | 含预期对象 |
| o05 | overview | verify | 含预期对象 |
| o06 | overview | verify | 含预期对象 |
| g01 | aggregate | verify |  |
| g02 | aggregate | verify |  |
| g03 | aggregate | verify |  |
| g04 | aggregate | verify |  |
| g05 | aggregate | verify |  |
| g06 | aggregate | verify |  |
| a01 | absent | abstained | 已拒答 |
| a02 | absent | abstained | 已拒答 |
| a03 | absent | abstained | 已拒答 |
| x01 | out_of_scope | abstained | 已拒答 |
| x02 | out_of_scope | abstained | 已拒答 |
| x03 | out_of_scope | abstained | 已拒答 |
