# 交接文档

> 自包含：读完这份就能继续开发。常驻约定见根目录 `AGENTS.md`。

**快照时间**：2026-09-20 · 版本 0.1.0（M0 + M1 完成）

## 1. 现状

国服《英雄联盟》版本更新公告的结构化问答：公告 → 结构化 chunk（数值改动 + 叙述）→ 版本感知检索 → 模板渲染 / 受限归纳，Jev 在候选上做重排。

- 语料：最近 10 个版本（26.9—26.18），1176 条（数值改动 500 条左右，叙述 670 段左右），86 个英雄、35 件装备出现在改动里。
- 检索：元数据过滤（版本/对象/类型/技能/模式）在打分之前；BM25 + 向量 + RRF。
- 决策：`jev.rerank` 对每条候选问一个 noul（是否正是用户问的那条），并用一个 choice 做相对选择；失败会降级并在 `session["trace"]` 留痕。
- 前端：`web/` 单页工作台（版本选择、证据列表、决策轨迹、准确性反馈）。
- 测试：59 项，无模型无网络可跑。

## 2. 环境与命令

| 项 | 值 |
|---|---|
| 项目根 | `D:\python项目\RAG Jev` |
| Python | `C:\Users\USER\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe` |
| 端口 | 18890 / 18891 / 18892 |
| 模型 | `models/`（junction 到旧项目 models，或 `RAGJEV_MODELS`） |

```powershell
$py = 'C:\Users\USER\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'
& $py -m unittest discover -s .\tests
& $py .\scripts\ingest_qq.py --offline --count 10
& $py .\launch.py --no-open          # 前台运行，等待 READY
& $py .\launch.py --stop
Invoke-RestMethod http://127.0.0.1:18890/api/health
```

## 3. 代码地图

| 文件 | 职责 | 关键符号 |
|---|---|---|
| `config.py` | 端口/路径单一来源 | `APP_PORT`、`MODELS`、`PATCH_DATA`、`KEY_FILE_ARG` |
| `patch_schema.py` | **解析与措辞的唯一定义处** | `FIELDS`、`ARROW_RE`、`CHANGE_LINE_RE`、`chunk_id`、`sentence`、`split_subject` |
| `scripts/ingest_qq.py` | 抓取/解析国服公告 → 语料 | `fetch_listing`、`resolve_patch`、`parse_article`、`main` |
| `scripts/build_aliases.py` | Data Dragon → 别名表 | `SLANG`、`learned_names` |
| `engine.py` | 检索内核 | `Retriever.search/all/_candidate_indices/gate`、`tokens`、`redact` |
| `patch_engine.py` | 版本解析、过滤、渲染、反馈 | `PatchRetriever`、`PatchEngine.parse/chat/_answer_subject/_answer_overview` |
| `jev.py` | TypeSafe 客户端 | `ask`、`rerank`、`classify`、`judge` |
| `server.py` | HTTP API + sqlite 会话 | `Handler`、`bootstrap`、`health` |
| `launch.py` | 三进程监督 | `windows_job`、`spawn`、`model_path` |
| `docs/解析覆盖率.md` | **解析质量台账**（每次 ingest 覆盖生成） | — |

## 4. 数据流

```
POST /api/chat
 └─ PatchEngine.chat
    ├─ parse：版本归一（26.17 / 16.17 / 16.17.1）→ 对象（别名表）→ 技能 → 字段 → 模式
    ├─ 边界：不在覆盖范围 / 超范围主题 / 无对象 → abstained 或 clarifying（带其它版本提示）
    ├─ Retriever.search(where=…)     # 元数据先过滤
    ├─ jев.rerank(hits[:20])         # noul 逐条 + choice 相对选择；失败降级并记 trace
    └─ 渲染：数值来自 chunk；模板句在 _render
```

## 5. 不变量

1. 数字只来自语料，`_render` 不接受模型输出。
2. 解析与措辞只在 `patch_schema.py` 定义，脚本与引擎共用。
3. 元数据过滤先于排名。
4. Jev 失败必须留痕，不能静默降级。
5. 公告原文不进仓库（`data/patch/raw/`、`knowledge.json`、`index.*` 已忽略）。
6. 新增行为必须带测试。

## 6. 已知限制

- 解析覆盖率不是 100%：26.10 有 17 条字段未归一到固定键（多为系统/符文条目），26.16 有 10 条技能字段无技能归属；这些行仍以原文保留，但不会被字段过滤命中。
- 叙述行（约占 57%）只用于检索与「同一对象的其他改动」展示，不参与数值回答。
- 26.9 的「任务进度/任务奖励」一类系统条目字段名很长，渲染时以原文标签呈现。
- 未做：英文公告对照核验、版本对比视图。
- 门槛：`GATE_SIMILARITY` / `GATE_BM25` **已测量但未采用**——范围内与越界问题在这份语料上相似度重叠
  （范围内最低 0.5458，越界最高 0.5980，见 `docs/门槛校准.md`）。范围判断由关键词黑名单与「没认出对象先追问」承担，
  阈值只出现在 `/api/health` 里。不要把它写进任何指标。

## 7. 下一步（按性价比排序）

1. **英文对照**：抓 en-us 公告，按对象集合做版本级对照核验（不一致不合并，只报告）。
2. **盲测集**：30—50 条与语料不同源的人工案例；当前 54 条案例同源，只能做回归不能外推。
3. **反馈回收**：`runtime/feedback.jsonl` 里的「有误」样本进评测集。
4. **版本对比视图**：前端加两版本并排 diff。

## 8. 已完成指标（回归基线）

`python scripts/evaluate_patch.py`（54 条同源案例 + 18 条规则陷阱题）：

| 指标 | bm25 | vector | hybrid | hybrid+jev |
|---|---|---|---|---|
| 定点问题命中@1 | 14/15 | 14/15 | 14/15 | **15/15** |
| 单点问题命中@1 | 28/28 | 28/28 | 28/28 | 28/28 |
| 汇总含预期对象 | 4/4 | 4/4 | 4/4 | 4/4 |
| 无记录/越界拒答 | 7/7 | 7/7 | 7/7 | 7/7 |
| 陷阱题（不同源） | 18/18 | 18/18 | 18/18 | 18/18 |

Jev 平均每次 $0.000038；差异只出现在「问题用词与字段标签不一致」的行（案例 h05），
不要把这条差异说成全量提升。

**断言核对**（0.3.0 新增，`_answer_subject` 里 `query["direction"]` 分支）：
- 断言与公告相反 → 纠正；
- 同一对象双向改动 → 两个方向都列；
- 列表问法（无 subject）才把方向当过滤条件。
这三条语义要一起改，否则「有哪些英雄被削弱」会退化成对单个英雄的核对。

英文对照（`docs/对照核验.md`）：10 个版本英雄对象双方一致 116 个、仅国服 0 个、仅英文 3 个。
仅英文的 26.9 Kennen/Teemo 需要抽查国服模式分区是否漏抽；26.14 Azir 是纯文字改动（无 ⇒ 行），
属于解析规则的有意取舍。装备差异来自英文的 `[NEW]/[REMOVED]/[REWORKED]` 标记，不是缺失。
