# 更新记录

## 0.1.0（2026-09-20）

首个可用版本：M0 骨架 + M1 语料。

**骨架（M0）**

- 从旧的「版本有据」项目移植检索内核（BM25 + 向量 + RRF + 元数据过滤）与三进程启动器（Windows Job Object 回收），删除了 Codex 排障领域。
- 端口改为 18890/18891/18892，避免与旧项目冲突。
- 新增 `jev.py`：TypeSafe System One 客户端（重排 / 分类 / 校验），无 key 或调用失败时降级并记录轨迹。
- 59 项单元测试，不需要模型与网络即可运行。

**语料（M1）**

- `scripts/build_aliases.py`：从 Data Dragon 生成英雄/装备别名表（625 个键），支持称号、英文名与 60+ 条玩家俗称；兼容 name/title 字段互换。
- `scripts/ingest_qq.py`：抓取国服版本更新公告，解析分区（英雄/装备/经典模式/大乱斗/竞技场）、技能行、数值变化行，生成结构化 chunk 与解析覆盖率报告。
- 收录最近 10 个版本（26.9—26.18），约 1170 条；数值变化约 500 条、公告叙述约 670 段。
- 版本号三轨归一：公告 26.17 ↔ Data Dragon 16.17.1。

**已验证**

- 端到端问答（含 Jev 重排）：`26.17 薇恩 W 真实伤害` 由 Jev 从 5 条候选中选出正确条目，单次约 $0.00003。
- 边界行为：未来版本拒答并给出覆盖范围；无记录时提示其它版本；皮肤/炫彩等明确超范围。

**未完成**

- 盲测集（当前 54 条案例同源）、反馈回收、前端版本对比视图。

## 0.2.0（2026-09-20）

M2：评测、门槛测量、英文对照。

**新增**

- `scripts/evaluate_patch.py`：54 条案例在 BM25 / 向量 / 混合 / 混合+Jev 四组上跑同一套指标，报告 `docs/评测报告.md`。
- `scripts/calibrate.py`：门槛测量。结论是**不采用**——范围内最低相似度 0.5458、越界最高 0.5980，区间重叠，写进 `docs/门槛校准.md`。
- `scripts/ingest_en.py`：英文公告对照核验，报告 `docs/对照核验.md`。
- 字段过滤由硬过滤改为**软偏好**：命中字段的行靠前，其余保留给 Jev 选择；会话记录 Jev 首选与成本。

**结果**

- 定点问题命中@1：BM25 / 向量 / 混合均为 14/15，**混合+Jev 15/15**（Jev 首选命中 11/11）。
- 单点、汇总、拒答四组指标四口径持平；Jev 平均每次 $0.000038。
- 对照核验：10 个版本英雄对象双方一致 116 个、仅国服 0 个、仅英文 3 个（26.9 Kennen/Teemo、26.14 Azir，后者是纯文字改动没有数值行）。

**修复**

- `暴击伤害` 一类字段先前被 `伤害` 规则抢先归类为 `damage`，现在按 `crit` 归类（需要重跑 ingest）。
- 新增超范围意图（写诗/攻略/出装/胜率等）的显式拒答，不再退回「没有找到」。

## 0.3.0（2026-09-20）

M3：断言核对与陷阱集（与语料不同源的唯一一组测量）。

**新增**

- **断言核对**：用户说「26.17 亚索被削了吗」时，系统对照语料回答，而不是顺着断言答。
  - 断言与公告相反 → 明确纠正（「没有被削弱：公告里是加强」）；
  - 同一对象两个方向都有 → 两个方向都列出（「既有削弱也有加强」），不选择性呈现；
  - 列表问法（「有哪些英雄被削弱」）仍然按方向过滤，两种语义分开处理。
- **字段缺失处理**：问了公告里没有的字段（如「亚索 Q 射程」）时，说明没有该记录并列出实际收录项，不再拿别的字段顶上。
- **只收国服**：问其它服务器数值时显式拒答。
- `tests/cases/trap_cases.json`：18 条陷阱题（错误断言、越界版本、越界主题、注入式输入、乱码、跨服务器、未收录字段、计算请求、皮肤）。

**结果**

- 陷阱题 **18/18**（四个口径一致）；定点命中@1 仍为 14/15（混合）与 15/15（混合+Jev）。
- 单元测试 65 项全绿（新增 6 项覆盖断言核对与边界）。

## 0.4.0（2026-09-20）

M3：查询理解盲测（与语料不同源的第二组测量）。

**新增**

- `tests/cases/parse_cases.json` + `scripts/evaluate_parse.py`：48 条只描述解析结果的案例
  （主体/版本/技能/字段/模式/意图/守卫），不看语料数值，**48/48 通过**，报告 `docs/查询理解.md`。
- 别名表纳入台服译名（Data Dragon `zh_TW`），键从 625 增到 941：`犽宿 → 亚索`、`翱銳龍獸 → 奥瑞利安·索尔`。

**修复（都是口语解析的真实缺口）**

- `大招/大绝/终极技能` → R 技能；`几秒/多少秒` → 冷却；`多少钱/多贵` → 价格。
- 拉丁名与版本号相邻时无法解析：`normalise` 之前把空格也删掉，`Yasuo 26.17` 粘成 `yasuo26.17`，
  现在保留空格作分隔符，`Yasuo 26.17 改了什么` 能同时解析出主体与版本。
- `写首关于亚索的诗` 这类长间隔的越界请求之前漏过关键词，现在命中超范围拒答。

**测试**：68 项（新增 3 项覆盖口语技能/字段与拉丁名解析）。

## 0.5.0（2026-09-20）

M3 收尾：回答自检（Jev 的最后一个接入点）。

**新增**

- `jev.judge_many`：一次调用为整条回答的每条改动问一个 noul——「证据是否支持这条陈述（同样的数字、同样的版本）」。
- `PatchEngine._self_check_lines`：把渲染出的每条改动与证据（语料句 + 结构化原值/新值 + 版本）对照，
  最低支持度 < 0.5 时在回答末尾加「自检：一致性偏低，请以公告原文为准」，并在轨迹里记录模型、条数、最低分与费用。
- 服务默认开启（`/api/health` 的 `jev.self_check`），`RAGJEV_SELF_CHECK=0` 可关闭；评测脚本只给 Jev 口径开启。

**结果**

- Jev 口径 47 条回答的自检最低支持度全部 ≥ 0.5（`docs/评测报告.md`）；自检成本约 $0.00002—0.00003/次。
- 早期版本曾误报 3 条：判断语句与语料句措辞不同被当成不一致，因此证据里补上了结构化数值，
  现在只有真的数字/版本对不上才会触发提示。

**测试**：71 项（新增 3 项覆盖自检触发、静默与默认关闭）。

## 0.6.0（2026-09-20）

M4：盲测入口、版本对比、反馈回收。

**新增**

- 盲测评测入口：`python scripts/evaluate_patch.py --cases tests\cases\blind_cases.json --out docs\盲测报告.md`，
  报告会把「系统答错」与「语料缺口」分开记账（预期值在语料里找不到的案例不计入分母）。
- `docs/盲测集提示词.md`：给 Codex 的出题提示词（禁止读语料、来源必须是官方公告、四种题型与字段约束、自检步骤）。
- 版本对比：`/api/patch/compare?a=&b=` + 工作台面板，按对象列出「改过 / 只在一版出现」的字段；
  公告只列发生改动的字段，面板里写明了这一点，避免被读成绝对数值表。
- `scripts/review_feedback.py`：读 `runtime/feedback.jsonl`，生成 `docs/反馈复核.md` 与可选的案例草稿。

**测试**：74 项（新增 3 项覆盖版本对比的 changed/added/removed 判定与叙述行排除）。

## 0.7.0（2026-09-20）

盲测集跑通，并修掉它暴露的四个真问题。

**盲测结果**（38 题，Codex 按官方公告出题、未接触语料；服务端为「混合检索 + Jev 重排」）

| 指标 | 无版本上下文 | 界面选中版本 |
|---|---|---|
| 单点数值：答案数值正确 | 19/20 | **20/20** |
| 单点数值：命中@1 / @5 | 18/20 · 19/20 | 19/20 · **20/20** |
| 版本正确（12 条写了版本的题） | 12/12 | 12/12 |
| 汇总含预期对象 | 7/8 | **8/8** |
| 无记录/越界拒答 | 10/10 | 10/10 |
| 回答自检（最低支持度 ≥ 0.5） | 13/13 | 20/20 |

报告：`docs/盲测报告.md`（无上下文）与 `docs/盲测报告-带版本上下文.md`。
剩余 1 条命中@1 未中：同一「被动暴击伤害」类字段在 26.13/26.14 各有一条，系统答了较新的那条。

**修复（盲测暴露）**

- `被动过` 被当成「被动技能」过滤器，导致「哪些英雄在这一版被动过」被过滤成空集 → 改为 `被动(?!过|了)`。
- 界面版本选择器实际是死代码：`query["patches"]` 总会填入默认版本，`not query["patches"]` 永不成立；
  现在改为「问题里没有明确版本时才采用下拉框选择」。
- 未写版本且最新版没有该改动时，不再直接说「没有记录」，而是回退到**最近一次改动**并标注「历史版本，
  不代表当前数值」（这正是玩家问「现在多少」时想要的答案）。
- 补 15 个常用俗称（牛头、男枪、女警、飞机、龙龟、猪妹、卡牌、小鱼人、大树、蜘蛛、蝎子、泰坦、三只手、宝石、虚空先知）。

**新增评测开关**

- `--use-case-version`：未写版本的题按案例版本作为界面选择传入，用来模拟用户在版本下拉框里的选择。
- `--cases / --out`：任意案例文件与报告路径（上一版加入）。

**测试**：80 项（新增 6 项覆盖选择器语义、被动过解析、最近改动回退、俗称表完整性）。

## 0.8.0（2026-09-20）

界面重做 + 两处检索/决策优化。

**界面（`web/` 全部重写）**

- 单页叙事结构：开场 → 问答 → 结构 → 数据 → 对比，左侧竖排索引随滚动高亮。
- 视觉：深色高对比、超大字号、画布粒子连线背景、扫描线与噪点纹理；答案按加强/削弱着色。
- 问答区右侧独立 **JEV 决策面板**：候选 noul 分数条（动画）、是否采用 Jev 顺序与分差、自检环形仪表、
  本次与累计 API 费用、模型耗时。
- 数据区：指标磁贴 + A/B 表格全部来自 `/api/evaluation`（读取 `docs/*.json`），页面不硬编码任何数字；
  另有「Jev 的净贡献（含负面结论）」清单。
- 动效遵守 `prefers-reduced-motion`。

**后端**

- 新增 `GET /api/evaluation`：汇总 `docs/评测报告.json`、`docs/盲测报告*.json`、`docs/查询理解.json`。
- `scripts/evaluate_patch.py` 与 `scripts/evaluate_parse.py` 现在同时写 JSON，供界面读取。
- **Jev 置信闸门**：候选 noul 分差 < 0.15 时不再改顺序（两条无法区分的候选同样正确）。
  盲测单点命中@1 因此从 19/20 回到 **20/20**；同源定点命中@1 保持 15/15。
- **公告说明回显**：问「为什么被调整」时，把该对象在该版本的公告叙述附在数值回答之后
  （叙述语料此前只参与检索、从不展示）。

**测试**：82 项（新增 2 项覆盖置信闸门的两侧行为）。

## 0.9.0（2026-09-20）

语料扩容 4 倍 + 统计表 + README 重写。

**语料**

- 从最近 10 个版本扩到 **30 个版本**（15.13—26.18）：chunk 1176 → **4995**，结构化改动 503 → **2212**，
  覆盖英雄 86 → 156、装备 29 → 58。
- 修复版本号双轨：2025 赛季国服公告同时用过 `15.x`（Data Dragon 写法）与 `25.x`（赛季前缀），
  `ddragon_map()` 与 `canonical_patch()` 改为「原样优先，再试 ±10」，30 个版本全部有正确映射。
- 公告列表元数据（日期 / URL）落盘 `raw/listing.json`，离线重建不再丢日期。

**新增**

- `scripts/corpus_stats.py` → `docs/语料统计.md`：扩容前后对比、逐版本明细、字段分布、分区与模式分布、解析质量口径。
- README 重写：差异化对比表、语料规模表、三口径评测表、Jev 净贡献表（含负面结论）。

**回归**

- 扩容后全部指标保持：盲测单点数值 20/20、命中@1 20/20、汇总 8/8、拒答 10/10、自检 20/20；
  同源定点命中@1 仍为 hybrid 14/15 → hybrid+Jev 15/15；查询理解 48/48；陷阱题 18/18。
- 说明：元数据过滤后候选仍只有 1–6 条，三种排序继续打平——这是「过滤先于排名」的结构性结果，不是缺陷。

## 0.10.0（2026-09-20）

跨版本聚合（多跳查询）。

**新增**

- 「赛娜从 26.13 到 26.18 一共改了几次」「亚恒历次改动有哪些」「沃利贝尔历次被削弱的记录」
  这类问题现在会给出**逐版本时间线**：每次改动一行、按版本排序，附方向统计（加强/削弱/调整）、
  最常被调整的字段，并明确写「这是历次调整的汇总，不等于当前实际数值」。
- 解析层新增 span（版本区间）与 ggregate 意图：识别「从…到…」「历次/一共/总共/改了几次/演变」，
  没写区间时按全部收录版本聚合；方向词（被削弱/被加强）作为过滤器，没有匹配时如实说明「没有该方向的记录」。
- 时间线保证**每个版本至少出现一行**再填充细节，避免长历史把最早或最新版本挤掉；
  被模式偏好略过的条目会单独说明（例：「另有 4 条来自其它模式：斗魂竞技场」）。

**评测**

- 新增 4 条聚合案例（kind: aggregate，检查时间线是否覆盖预期版本）：四口径 **4/4**。
- 其余指标不变：盲测单点数值 20/20、命中@1 20/20、汇总 8/8、拒答 10/10、自检 20/20；
  同源定点命中@1 hybrid 14/15 → hybrid+Jev 15/15；陷阱 18/18。
- 测试 89 项（新增 5 项覆盖区间解析、方向收窄、无匹配方向的说明、未知对象仍追问）。

## 0.11.0（2026-09-20）

第二批盲测（40 题，覆盖 15.13—26.8）+ 它抓到的 3 个系统性缺陷。

**盲测（合并 78 题）**

| 指标 | 结果 |
|---|---|
| 单点数值正确 | **40/40** |
| 检索命中@1 / @5 | **40/40 · 40/40** |
| 版本正确（26 条写了版本） | **26/26** |
| 汇总含预期对象 | **14/14** |
| 跨版本聚合时间线覆盖 | **6/6** |
| 无记录 / 越界拒答 | **16/16** |
| Jev 回答自检 | **40/40** |

**修复（第二批盲测暴露）**

- 方向判定改为逐个数对比（含混合取和）：`30-60% → 30-50%` 现在正确判为削弱，`adjust` 桶从约 700 条降到 81 条。
- 数字正则加前置断言：区间连字符不再被当成负号（`30-60%` 之前解析出 `[30, -60]`）。
- 解析时从字段名前缀提取技能（`R 被动过载涌动伤害` → 技能 R），早期版本「无技能归属」的行因此减少。
- 别名匹配改为「最长优先 → 位置靠前优先 → 英雄优先于装备」：提问里的物品名不会再压过英雄名。
- 俗称表补 15 条（船长/鸟皇/熊/猴子/诺手/铁男/冰女/稻草人/炼金/炸弹人/蛇女/扇子妈/机器人/潘森/兰博）。

**语料缺口（单独记账，不算系统答错）**

- 15.16 格温基础护甲：国服公告没有这条（英文有）——两边公告内容差异。
- 26.6 希瓦娜大型更新：重做章节是描述性文字，没有 `旧值 ⇒ 新值`，结构化解析按设计不收录。

**测试**：93 项（新增 4 项覆盖区间方向、混合列表、连字符、别名优先级）。

## 0.12.0（2026-09-20）

把「Jev 的加成」变成三个可复现实验。

**新增**

- `scripts/evaluate_jev.py`：逐条记账 —— 83 条可判定案例里，Jev 改序 1 条（改对 1）、保留 82 条、
  39 条因候选不足 2 条无从介入；`--ranker` 模式额外测「去掉字段预过滤、同一批候选」的排序能力：
  BM25 47/49 → Jev **48/49**（Jev 独对 1、BM25 独对 0）。报告 `docs/Jev效果.md`。
- `scripts/evaluate_selfcheck.py`：注入检出率 —— 把已答对回答的最后一个数字改掉 1 再送自检：
  对照支持度 0.96、注入 0.04，**检出 24/24、误报 0/24**，两次调用 $0.00045。报告 `docs/Jev自检.md`。
- 会话记录 `retrieval_top` / `after_top` / `decisive` / `gap`，工作台 Jev 面板直接显示
  「检索第 1 → Jev 第 1」的对照；数据区新增三项 Jev 实验数字（读 `/api/evaluation`）。
- README 新增「Jev 到底加成了什么」：三个实验 + 值/不值/成本三段结论。

**测试**：93 项（不变）。

## 0.13.0（2026-09-21）

查询体验：把「明明有却查不到」的场景补齐，并按 taste skill 重做界面。

**新增能力**

- **技能名提问**：`scripts/build_abilities.py` 从 Data Dragon championFull 生成 865 条技能名
  （173 个英雄 × Q/W/E/R/被动），解析层支持「锐雯的放逐之锋」「亚索的浪客之道」这类问法。
  可选与另一份 Data Dragon 导出的术语词典交叉核对（`RAGJEV_TERMS` 指向该文件）：本次核对 **864/864 重合**。
- **俗称与缩写扩容**：装备俗称规则 74 条（含 IE/BT/QSS/PD/ROA/TF/BotRK/Zhonya 等英文缩写），
  英雄俗称补到 100+ 条（含瑞雯/维恩/卡密尔/卢西安等高频异写）；规则命中不到装备时会打印出来。
- **历史名与内部代号**：语料里出现、但 Data Dragon 已改名或未收录的对象（卢登的配枪、C44、不朽之路）
  由 `learned_subjects.json` 自动补入别名表。
- **分区类型修正**：模式分区（斗魂竞技场等）里的英雄/装备不再被标成 `mode`，改为按别名表推断；
  主体已解析时查询不再按 `type` 过滤——否则竞技场里的电刀改动会被漏掉。
- **未收录时的模糊提示**：认不出对象时用编辑距离给出「你是不是想问 X」（只提示语料里真能回答的对象）。

**修正的假阳性**

- 公告只有说明文字、没有数值条目时（26.16 迦娜），不再把说明当改动答，改为明确「不给数值」。
- 重做/大型更新（26.6 希瓦娜）如实说明不收录，不再顺着问题给一条无关改动。

**界面**

- 按 taste skill 重做：去掉 AI 紫蓝渐变与每节 eyebrow 编号，改为**单一琥珀金强调色**（Hextech 语汇）、
  非对称布局、卡片/控件/胶囊三档圆角；新增独立的「Jev 到底加成了什么」区块（自检点图 + 排序对照 + 记账条），
  语料与评测图表同区；顶部滚动进度、章节激活态、视差与图表生长动画保留并遵守 `prefers-reduced-motion`。

**回归**

- 盲测 78 题：单点数值 40/40、命中@1 40/40、汇总 14/14、聚合 6/6、拒答 16/16、自检 40/40。
- 同源 84 例：陷阱 18/18、聚合 4/4、定点命中@1 21/23 → 22/23（Jev）、自检 55/55。
- 测试 98 项。
