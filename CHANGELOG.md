# 更新记录

## 0.1.0（2026-09-20）

首个可用版本：M0 骨架 + M1 语料。

**骨架（M0）**

- 从旧的「版本有据」项目移植检索内核（BM25 + 向量 + RRF + 元数据过滤）与三进程启动器（Windows Job Object 回收），删除了 Codex 排障领域。
- 端口改为 18890/18891/18892，避免与旧项目冲突。
- 新增 `jev.py`：TypeSafe System One 客户端（重排 / 分类 / 校验），无 key 或调用失败时降级并记录轨迹。
- 59 项单元测试，不需要模型与网络即可运行。

**语料（M1）**

- `scripts/build_aliases.py`：从 Data Dragon 生成英雄/装备别名表（625 个键），支持称号、英文名与 60+ 条玩家俗称；兼容 name/title 字段互换。
- `scripts/ingest_qq.py`：抓取国服版本更新公告，解析分区（英雄/装备/经典模式/大乱斗/竞技场）、技能行、数值变化行，生成结构化 chunk 与解析覆盖率报告。
- 收录最近 10 个版本（26.9—26.18），约 1170 条；数值变化约 500 条、公告叙述约 670 段。
- 版本号三轨归一：公告 26.17 ↔ Data Dragon 16.17.1。

**已验证**

- 端到端问答（含 Jev 重排）：`26.17 薇恩 W 真实伤害` 由 Jev 从 5 条候选中选出正确条目，单次约 $0.00003。
- 边界行为：未来版本拒答并给出覆盖范围；无记录时提示其它版本；皮肤/炫彩等明确超范围。

**未完成**

- 盲测集（当前 54 条案例同源）、反馈回收、前端版本对比视图。

## 0.2.0（2026-09-20）

M2：评测、门槛测量、英文对照。

**新增**

- `scripts/evaluate_patch.py`：54 条案例在 BM25 / 向量 / 混合 / 混合+Jev 四组上跑同一套指标，报告 `docs/评测报告.md`。
- `scripts/calibrate.py`：门槛测量。结论是**不采用**——范围内最低相似度 0.5458、越界最高 0.5980，区间重叠，写进 `docs/门槛校准.md`。
- `scripts/ingest_en.py`：英文公告对照核验，报告 `docs/对照核验.md`。
- 字段过滤由硬过滤改为**软偏好**：命中字段的行靠前，其余保留给 Jev 选择；会话记录 Jev 首选与成本。

**结果**

- 定点问题命中@1：BM25 / 向量 / 混合均为 14/15，**混合+Jev 15/15**（Jev 首选命中 11/11）。
- 单点、汇总、拒答四组指标四口径持平；Jev 平均每次 $0.000038。
- 对照核验：10 个版本英雄对象双方一致 116 个、仅国服 0 个、仅英文 3 个（26.9 Kennen/Teemo、26.14 Azir，后者是纯文字改动没有数值行）。

**修复**

- `暴击伤害` 一类字段先前被 `伤害` 规则抢先归类为 `damage`，现在按 `crit` 归类（需要重跑 ingest）。
- 新增超范围意图（写诗/攻略/出装/胜率等）的显式拒答，不再退回「没有找到」。

## 0.3.0（2026-09-20）

M3：断言核对与陷阱集（与语料不同源的唯一一组测量）。

**新增**

- **断言核对**：用户说「26.17 亚索被削了吗」时，系统对照语料回答，而不是顺着断言答。
  - 断言与公告相反 → 明确纠正（「没有被削弱：公告里是加强」）；
  - 同一对象两个方向都有 → 两个方向都列出（「既有削弱也有加强」），不选择性呈现；
  - 列表问法（「有哪些英雄被削弱」）仍然按方向过滤，两种语义分开处理。
- **字段缺失处理**：问了公告里没有的字段（如「亚索 Q 射程」）时，说明没有该记录并列出实际收录项，不再拿别的字段顶上。
- **只收国服**：问其它服务器数值时显式拒答。
- `tests/cases/trap_cases.json`：18 条陷阱题（错误断言、越界版本、越界主题、注入式输入、乱码、跨服务器、未收录字段、计算请求、皮肤）。

**结果**

- 陷阱题 **18/18**（四个口径一致）；定点命中@1 仍为 14/15（混合）与 15/15（混合+Jev）。
- 单元测试 65 项全绿（新增 6 项覆盖断言核对与边界）。

## 0.4.0（2026-09-20）

M3：查询理解盲测（与语料不同源的第二组测量）。

**新增**

- `tests/cases/parse_cases.json` + `scripts/evaluate_parse.py`：48 条只描述解析结果的案例
  （主体/版本/技能/字段/模式/意图/守卫），不看语料数值，**48/48 通过**，报告 `docs/查询理解.md`。
- 别名表纳入台服译名（Data Dragon `zh_TW`），键从 625 增到 941：`犽宿 → 亚索`、`翱銳龍獸 → 奥瑞利安·索尔`。

**修复（都是口语解析的真实缺口）**

- `大招/大绝/终极技能` → R 技能；`几秒/多少秒` → 冷却；`多少钱/多贵` → 价格。
- 拉丁名与版本号相邻时无法解析：`normalise` 之前把空格也删掉，`Yasuo 26.17` 粘成 `yasuo26.17`，
  现在保留空格作分隔符，`Yasuo 26.17 改了什么` 能同时解析出主体与版本。
- `写首关于亚索的诗` 这类长间隔的越界请求之前漏过关键词，现在命中超范围拒答。

**测试**：68 项（新增 3 项覆盖口语技能/字段与拉丁名解析）。

## 0.5.0（2026-09-20）

M3 收尾：回答自检（Jev 的最后一个接入点）。

**新增**

- `jev.judge_many`：一次调用为整条回答的每条改动问一个 noul——「证据是否支持这条陈述（同样的数字、同样的版本）」。
- `PatchEngine._self_check_lines`：把渲染出的每条改动与证据（语料句 + 结构化原值/新值 + 版本）对照，
  最低支持度 < 0.5 时在回答末尾加「自检：一致性偏低，请以公告原文为准」，并在轨迹里记录模型、条数、最低分与费用。
- 服务默认开启（`/api/health` 的 `jev.self_check`），`RAGJEV_SELF_CHECK=0` 可关闭；评测脚本只给 Jev 口径开启。

**结果**

- Jev 口径 47 条回答的自检最低支持度全部 ≥ 0.5（`docs/评测报告.md`）；自检成本约 $0.00002—0.00003/次。
- 早期版本曾误报 3 条：判断语句与语料句措辞不同被当成不一致，因此证据里补上了结构化数值，
  现在只有真的数字/版本对不上才会触发提示。

**测试**：71 项（新增 3 项覆盖自检触发、静默与默认关闭）。

## 0.6.0（2026-09-20）

M4：盲测入口、版本对比、反馈回收。

**新增**

- 盲测评测入口：`python scripts/evaluate_patch.py --cases tests\cases\blind_cases.json --out docs\盲测报告.md`，
  报告会把「系统答错」与「语料缺口」分开记账（预期值在语料里找不到的案例不计入分母）。
- `docs/盲测集提示词.md`：给 Codex 的出题提示词（禁止读语料、来源必须是官方公告、四种题型与字段约束、自检步骤）。
- 版本对比：`/api/patch/compare?a=&b=` + 工作台面板，按对象列出「改过 / 只在一版出现」的字段；
  公告只列发生改动的字段，面板里写明了这一点，避免被读成绝对数值表。
- `scripts/review_feedback.py`：读 `runtime/feedback.jsonl`，生成 `docs/反馈复核.md` 与可选的案例草稿。

**测试**：74 项（新增 3 项覆盖版本对比的 changed/added/removed 判定与叙述行排除）。
